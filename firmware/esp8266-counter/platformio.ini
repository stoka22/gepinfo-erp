[platformio]
default_envs = esp8266_debug

; Shared by every [env:*] section below unless explicitly overridden there.
; This is a SEPARATE, transitional project -- see README.md. It shares no
; source files with ../esp32-counter on purpose.
[env]
platform = espressif8266
; Az ezen a panelen ellenorzott hardver: csupasz ESP-WROOM-DEV modul,
; ESP8266EX, 4MB flash (esptool flash_id-vel merve). Az esp12e board-
; definicio (dio flash-mod, 4m1m ldscript, nodemcu pin-varians) pontosan
; ezt a konfiguraciot adja, csak nem Wemos-specifikus cimkezessel -- lasd
; D1/D2/D5/D6 makrok a config_local.h-ban valtozatlanul mukodnek.
board = esp12e
framework = arduino
monitor_speed = 115200
upload_speed = 460800
; A BearSSL TLS-kezfogas (postSample()) + egyidejuleg futo ArduinoOTA/mDNS
; a default 4KB "cont" stacket kimeriti -- Soft WDT reset / Exception(4)
; hardveresen reprodukalhato volt (COM3, 2026-09-24), pontosan az elso
; olyan push-nal, amikor mar volt api_key ES az OTA is aktiv. Ismert
; ESP8266 Arduino core korlat, a hivatalos workaround a nagyobb stack.
build_flags =
    -DCONT_STACKSIZE=8192
lib_deps =
    bblanchon/ArduinoJson @ ^6.21.5

; USB upload, release build. This is the env that produces OTA-catalog
; binaries, hence the post-build copy into ota/firmware-<version>.bin.
[env:esp8266]
extra_scripts = post:extra_script.py

; Over-the-air upload once the device has enrolled and has OTA credentials.
[env:esp8266_ota]
upload_protocol = espota
upload_port = ESP8266_COUNTER.local

; USB upload, debug build.
[env:esp8266_debug]
build_type = debug
build_flags =
    -DDEBUG_ESP_PORT=Serial
